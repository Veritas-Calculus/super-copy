# SuperCopy

一款 Android 离线 OCR 工具，通过截图识别文字并自动复制到剪贴板。

## ✨ 功能特性

- 🖼️ **截图 OCR 识别** - 通过悬浮按钮快速截图，识别文字后自动复制到剪贴板
- 📴 **完全离线** - 使用 Google ML Kit，所有处理均在本地完成，无需网络
- 📋 **剪贴板历史** - 支持查看和管理历史识别记录
- 🔒 **隐私安全** - 不上传任何数据，每次截图需用户授权
- 🌐 **多语言支持** - 支持中文、英文等多种语言识别

## 📱 使用方法

1. 打开应用，点击"显示悬浮按钮"
2. 授予悬浮窗权限
3. 在任意应用中点击悬浮按钮进行截图
4. 授权后自动识别文字并复制到剪贴板
5. 可在"历史记录"中查看之前的识别结果

## 📋 系统要求

- Android 10 (API 29) 及以上版本
- 需要悬浮窗权限

## 🛠️ 技术栈

- **语言**: Kotlin 2.0
- **UI**: Jetpack Compose + Material3
- **数据库**: Room
- **OCR**: Google ML Kit (Text Recognition)
- **最低 SDK**: 29 (Android 10)
- **目标 SDK**: 35 (Android 15)
- **构建工具**: Gradle 8.9.2 + KSP

## 🔨 构建说明

### 环境准备

1. 安装 Android Studio Ladybug 或更高版本
2. 确保已配置 Android SDK (compileSdk 36)
3. JDK 17 或更高版本

### 编译项目

```bash
# 克隆项目
git clone https://github.com/Veritas-Calculus/super-copy.git
cd super-copy

# 编译 Debug 版本
./gradlew assembleDebug

# 编译 Release 版本
./gradlew assembleRelease
```

### 安装到设备

```bash
./gradlew installDebug
```

## 📁 项目结构

```
super-copy/
├── app/                          # 主应用模块
│   ├── src/main/
│   │   ├── java/.../super_copy/
│   │   │   ├── data/            # 数据层 (Repository, Database, DAO)
│   │   │   ├── ocr/             # OCR 识别模块
│   │   │   ├── service/         # 服务 (悬浮按钮、截图)
│   │   │   ├── ui/              # UI 组件 (Compose)
│   │   │   ├── MainActivity.kt
│   │   │   └── ScreenCaptureActivity.kt
│   │   └── res/                 # 资源文件
│   └── build.gradle.kts
├── gradle/
│   └── libs.versions.toml       # 依赖版本管理
├── build.gradle.kts             # 根构建脚本
├── settings.gradle.kts          # 项目设置
├── LICENSE                      # MIT 许可证
└── readme.md
```

## 📄 许可证

本项目采用 [MIT License](LICENSE) 开源协议。

## 🙏 致谢

- [Google ML Kit](https://developers.google.com/ml-kit) - 提供离线 OCR 能力
- [Jetpack Compose](https://developer.android.com/jetpack/compose) - 现代化 UI 框架
